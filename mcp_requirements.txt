# Prometheus Prime - MCP Integration Requirements

# MCP SDK
mcp>=0.1.0

# Core dependencies (if not already installed from main requirements.txt)
asyncio
aiohttp>=3.8.0
requests>=2.28.0
python-dotenv>=0.19.0

# Optional for enhanced functionality
flask>=2.0.0
flask-cors>=3.0.10
